const express = require('express');
const router = express.Router();
const eventCtrl = require('../../controllers/events');

/*---------- Public Routes ----------*/
// router.post('/', eventCtrl.currentEvents);
// router.post('/getEvent', eventCtrl.getEvent);

module.exports = router;
